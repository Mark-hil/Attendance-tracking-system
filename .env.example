# Database settings
DB_NAME=your_db_name
DB_USER=your_db_user
DB_PASSWORD=your_db_password
DB_HOST=db
DB_PORT=5432

# Django settings
SECRET_KEY=your-secret-key-here
DEBUG=True
ALLOWED_HOSTS=localhost,127.0.0.1

# Email settings (if needed)
# EMAIL_HOST=your-smtp-host
# EMAIL_PORT=587
# EMAIL_USE_TLS=True
# EMAIL_HOST_USER=your-email@example.com
# EMAIL_HOST_PASSWORD=your-email-password

# how can i prevent the used of other qrcode  
# scanner from scanning and recording the attendance. but only used the scanner in the app 
prevent the member ID and token from being visible when scanning with any QR code scanner.